services:
  - type: web
    name: idchaha
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && cd react-app && npm install && npm run build
    startCommand: python server.py
    envVars:
      - key: HENRIK_API_KEYS
        sync: false
